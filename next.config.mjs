/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cache.pnix.exchange',
      'gcdn.wemade.games',
      'cache.wemixplay.com',
      'live-dl.nightcrows.com',
    ],
  },
}

export default nextConfig
